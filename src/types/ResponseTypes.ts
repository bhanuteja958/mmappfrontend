export interface Transaction {
    id: string
    amount: number
    category: string
    created_date: string
    description: string
    modified_date: string
    payment_mode: string
    transaction_type: string
}